import React from 'react';
import classes from './DrawerToggle.css';

const draweraToggle = (props) => (
    <div className={classes.DrawerToggle} onClick={props.clicked}> Manu </div>
);

export default draweraToggle;